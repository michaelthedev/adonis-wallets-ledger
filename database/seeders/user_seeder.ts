import { BaseSeeder } from '@adonisjs/lucid/seeders'

export default class extends BaseSeeder {
  async run() {
    // 2 default users with wallets
    const users = [
      {
        
      }
    ]
  }
}
